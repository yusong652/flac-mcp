{
  "name": "stress-initialization",
  "dimension": "mixed",
  "full_name": "Stress Initialization",
  "description": "Set initial zone stresses directly or compute gravitational in-situ stresses before solving. Initial stresses should be compatible with boundary conditions and the assigned constitutive model.",
  "primary_commands": [
    "model gravity",
    "zone initialize",
    "zone initialize-stresses",
    "zone face apply",
    "model solve elastic"
  ],
  "methods": [
    {
      "name": "uniform stress",
      "commands": [
        "zone initialize stress-xx -5e6",
        "zone initialize stress-yy -1e7",
        "zone initialize stress-zz -5e6"
      ],
      "notes": [
        "Use range clauses to assign different initial stresses to different regions.",
        "Match stress boundaries when boundaries are stress controlled."
      ]
    },
    {
      "name": "gravity stress with K0 ratio",
      "commands": [
        "model gravity 10",
        "zone initialize-stresses ratio 0.5"
      ],
      "notes": [
        "Assign density before using gravity stress initialization.",
        "Use different ratio values or overburden where geology requires it."
      ]
    },
    {
      "name": "manual gradient",
      "commands": [
        "zone initialize stress-xx -1e6 gradient (0,0,10000)",
        "zone initialize stress-yy -1e6 gradient (0,0,10000)"
      ],
      "notes": [
        "Gradient values are evaluated from zone centroid positions.",
        "FLAC2D models can initialize plane-strain stress-zz, but should not initialize z velocity."
      ]
    }
  ],
  "validation_checks": [
    "Run a small number of steps or 'model solve elastic' and inspect velocities/unbalanced force.",
    "Plot zone state or plastic indicators to detect immediate yield from incompatible initial stresses.",
    "After gravity initialization, reset displacements if the subsequent staged analysis needs zeroed movement."
  ],
  "official_documentation": "https://docs.itascacg.com/itasca900/flac3d/docproject/source/modeling/problemsolving/initial_conditions.html"
}
