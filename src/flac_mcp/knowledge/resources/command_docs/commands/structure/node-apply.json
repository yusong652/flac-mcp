{
  "category": "structure",
  "search_keywords": [
    "node",
    "apply",
    "structure"
  ],
  "description": "Set generalized point loads for all nodes in the range. Generalized point loads consist of an applied force and an applied moment. Pressure loading can be applied to shell surfaces using the commands structure shell apply , structure geogrid apply , or structure liner apply . Distributed loads can be applied to beam and pile surfaces using the command structure beam apply or structure pile apply . The following keywords are available to define generalized point loads.",
  "python_sdk_alternative": {
    "available": false
  },
  "versions": {
    "6.0": {
      "command": "structure node apply",
      "syntax": "structure node apply keyword <range>",
      "description": "Set generalized point loads for all nodes in the range. Generalized point loads consist of an applied force and an applied moment. Pressure loading can be applied to shell surfaces using the commands structure shell apply, structure geogrid apply, or structure liner apply. Distributed loads can be applied to beam and pile surfaces using the command structure beam apply or structure pile apply. The following keywords are available to define generalized point loads.",
      "keywords": [
        {
          "name": "force",
          "syntax": "force v <keyword>",
          "description": "applied force vector (in terms of system defined by the system keyword). You can use one of the following keywords to modify how v is applied to the node:"
        },
        {
          "name": "force-add",
          "syntax": "add",
          "description": "the vector v is added to the existing applied force vector at that node"
        },
        {
          "name": "force-multiply",
          "syntax": "multiply",
          "description": "the vector v is multiplied to the existing applied force vector at that node in a component by component multiplication"
        },
        {
          "name": "force-edge",
          "syntax": "force-edge v <add>",
          "description": "by default, these forces will overwrite the previous applied forces at the node; however, the add keyword can be given to add them to those currently present. Note that if this changes the system they were applied under, the previous forces will not be corrected for the system change."
        },
        {
          "name": "moment",
          "syntax": "moment v <keyword>",
          "description": "applied moment vector (in terms of system defined by the system keyword). The following keywords can be used to modify how v is applied to the node:"
        },
        {
          "name": "moment-add",
          "syntax": "add",
          "description": "the vector v is added to the existing applied moment vector at that node"
        },
        {
          "name": "moment-multiply",
          "syntax": "multiply",
          "description": "the vector v is multiplied to the existing applied moment vector at that node in a component by component multiplication"
        },
        {
          "name": "remove",
          "syntax": "remove keyword",
          "description": "an applied condition remains in effect until it is removed with this command. The following keywords are available to choose which applied condition is removed:"
        },
        {
          "name": "remove-force",
          "syntax": "force",
          "description": "removes any applied force condition on the node"
        },
        {
          "name": "remove-moment",
          "syntax": "moment",
          "description": "removes any applied moment condition on the node"
        },
        {
          "name": "system",
          "syntax": "system keyword",
          "description": "set the coordinate system in which the force and moment vectors are expressed. The default system is global. If the system is set to local, then in large-strain mode, the force and moment vectors will remain aligned with the local system as it rotates."
        },
        {
          "name": "system-local",
          "syntax": "local"
        },
        {
          "name": "system-global",
          "syntax": "global"
        }
      ],
      "legacy_documentation": {
        "availability_verified": true,
        "source": "official FLAC3D 6.0 command indexes",
        "source_urls": [
          "https://docs.itascacg.com/pfc600/flac3d/sel/doc/manual/sel_manual/nodes/commands/cmd_structure.node.apply.html#structure.node.apply"
        ],
        "syntax_basis": "official FLAC3D 6.0 detailed command page",
        "target_version": "6.0"
      }
    },
    "7.0": {
      "command": "structure node apply",
      "syntax": "structure node apply keyword <range>",
      "description": "Set generalized point loads for all nodes in the range. Generalized point loads consist of an applied force and an applied moment. Pressure loading can be applied to shell surfaces using the commands structure shell apply, structure geogrid apply, or structure liner apply. Distributed loads can be applied to beam and pile surfaces using the command structure beam apply or structure pile apply. The following keywords are available to define generalized point loads.",
      "keywords": [
        {
          "name": "force",
          "syntax": "force v <keyword>",
          "description": "applied force vector (in terms of system defined by the system keyword). You can use one of the following keywords to modify how v is applied to the node:"
        },
        {
          "name": "force-add",
          "syntax": "add",
          "description": "the vector v is added to the existing applied force vector at that node"
        },
        {
          "name": "force-multiply",
          "syntax": "multiply",
          "description": "the vector v is multiplied to the existing applied force vector at that node in a component by component multiplication"
        },
        {
          "name": "force-edge",
          "syntax": "force-edge v <add>",
          "description": "by default, these forces will overwrite the previous applied forces at the node; however, the add keyword can be given to add them to those currently present. Note that if this changes the system they were applied under, the previous forces will not be corrected for the system change."
        },
        {
          "name": "moment",
          "syntax": "moment v <keyword>",
          "description": "applied moment vector (in terms of system defined by the system keyword). The following keywords can be used to modify how v is applied to the node:"
        },
        {
          "name": "moment-add",
          "syntax": "add",
          "description": "the vector v is added to the existing applied moment vector at that node"
        },
        {
          "name": "moment-multiply",
          "syntax": "multiply",
          "description": "the vector v is multiplied to the existing applied moment vector at that node in a component by component multiplication"
        },
        {
          "name": "remove",
          "syntax": "remove keyword",
          "description": "an applied condition remains in effect until it is removed with this command. The following keywords are available to choose which applied condition is removed:"
        },
        {
          "name": "remove-force",
          "syntax": "force",
          "description": "removes any applied force condition on the node"
        },
        {
          "name": "remove-moment",
          "syntax": "moment",
          "description": "removes any applied moment condition on the node"
        },
        {
          "name": "system",
          "syntax": "system keyword",
          "description": "set the coordinate system in which the force and moment vectors are expressed. The default system is global. If the system is set to local, then in large-strain mode, the force and moment vectors will remain aligned with the local system as it rotates."
        },
        {
          "name": "system-local",
          "syntax": "local"
        },
        {
          "name": "system-global",
          "syntax": "global"
        }
      ],
      "legacy_documentation": {
        "availability_verified": true,
        "source": "official FLAC3D 7.0 contents command index",
        "source_urls": [
          "https://docs.itascacg.com/flac3d700/common/sel/doc/manual/sel_manual/nodes/commands/cmd_structure.node.apply.html"
        ],
        "syntax_basis": "official FLAC3D 7.0 detailed command page",
        "target_version": "7.0"
      }
    },
    "9.0": {
      "command": "structure node apply",
      "syntax": "structure node apply keyword [range]",
      "keywords": [
        {
          "name": "force-multiply",
          "syntax": "multiply",
          "description": "applied force vector (in terms of system defined by the system keyword). You can use one of the following keywords to modify how v is applied to the node: the vector v is added to the existing applied force vector at that node the vector v is multiplied to the existing applied force vector at that node in a component by component multiplication"
        },
        {
          "name": "force-edge",
          "syntax": "force-edge <v>[add]",
          "description": "the vector v is added as a force-per-unit-length to all edges that join nodes in the range. The forces assigned due to the edge will be lumped at the two nodes, with each node receiving half. These forces are always in the global system and will set the nodes to the global system as with the structure node apply system global command. These forces will overwrite the previous applied forces at the node; however, the add keyword can be given to add them to those currently present. Note that if this changes the system they were applied under, then the previous forces will not be corrected for the system change."
        },
        {
          "name": "moment-multiply",
          "syntax": "multiply",
          "description": "applied moment vector (value in 2D) (in terms of system defined by the system keyword). The following keywords can be used to modify how v ( f in 2D) is applied to the node: the vector v ( f in 2D) is added to the existing applied moment vector at that node the vector v ( f in 2D) is multiplied to the existing applied moment vector at that node in a component by component multiplication"
        },
        {
          "name": "remove-moment",
          "syntax": "moment",
          "description": "an applied condition remains in effect until it is removed with this command. The following keywords are available to choose which applied condition is removed: removes any applied force condition on the node removes any applied moment condition on the node"
        },
        {
          "name": "system-global",
          "syntax": "global",
          "description": "set the coordinate system in which the force and moment vectors are expressed. The default system is global. If the system is set to local, then in large-strain mode, the force and moment vectors will remain aligned with the local system as it rotates."
        }
      ]
    }
  }
}
