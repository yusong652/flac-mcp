{
  "category": "model",
  "search_keywords": [
    "orientation",
    "tracking",
    "euler"
  ],
  "description": "Specify the orientation tracking state. When enabled, the orientation of objects is tracked during rotation, allowing access to Euler angles and orientation-based attributes.",
  "notes": [
    "When enabled: orientation of objects is tracked during rotation",
    "Allows access to Euler angles (euler-x, euler-y, euler-z in 3D)",
    "Allows access to orientation-based attributes",
    "Must be enabled BEFORE rotating objects if Euler angles are needed",
    "Adds computational overhead - only enable if orientation information is required",
    "  - euler-x, euler-y, euler-z (3D only)",
    "  - rotation (2D)",
    "Default state: typically off (check documentation)",
    "Orientation tracking state persists until changed or model new"
  ],
  "python_sdk_alternative": {
    "available": false,
    "workaround": "itasca.command('model orientation-tracking on') or itasca.command('model orientation-tracking off') - Python SDK does not provide direct orientation tracking control"
  },
  "versions": {
    "7.0": {
      "command": "model orientation-tracking",
      "syntax": "model orientation-tracking <b>",
      "examples": [
        {
          "command": "model orientation-tracking on"
        },
        {
          "command": "model orientation-tracking off",
          "description": "Disable orientation tracking (reduces computational overhead)"
        },
        {
          "command": "model orientation-tracking on\n; ... create and rotate objects ...",
          "description": "Enable tracking to access Euler angles in FISH"
        }
      ]
    },
    "6.0": {
      "command": "model orientation-tracking",
      "syntax": "model orientation-tracking <b>",
      "examples": [
        {
          "command": "model orientation-tracking on"
        },
        {
          "command": "model orientation-tracking off",
          "description": "Disable orientation tracking (reduces computational overhead)"
        },
        {
          "command": "model orientation-tracking on\n; ... create and rotate objects ...",
          "description": "Enable tracking to access Euler angles in FISH"
        }
      ]
    },
    "9.0": {
      "command": "model orientation-tracking",
      "syntax": "model orientation-tracking <b>",
      "examples": [
        {
          "command": "model orientation-tracking on"
        },
        {
          "command": "model orientation-tracking off",
          "description": "Disable orientation tracking (reduces computational overhead)"
        },
        {
          "command": "model orientation-tracking on\n; ... create and rotate objects ...",
          "description": "Enable tracking to access Euler angles in FISH"
        }
      ]
    }
  }
}
