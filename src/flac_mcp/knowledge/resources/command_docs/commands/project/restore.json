{
  "category": "project",
  "search_keywords": [
    "restore"
  ],
  "description": "Restores the project file name s . If a file extension is not specified, the extension prj will be assumed. This will clear the current project (including the current model state) and load the specified project.",
  "python_sdk_alternative": {
    "available": false
  },
  "versions": {
    "6.0": {
      "command": "project restore",
      "syntax": "project restore <s>"
    },
    "7.0": {
      "command": "project restore",
      "syntax": "project restore <s>"
    },
    "9.0": {
      "command": "project restore",
      "syntax": "project restore <s>"
    }
  }
}
